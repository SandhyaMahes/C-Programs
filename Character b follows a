#include<stdio.h>
#include <stdlib.h>
#include<string.h>
int main()
{char s[200],a,b;
int i=0,j=0,n;
scanf("%s\n",s);//'\n' should be used
scanf("%c\n",&a);
scanf("%c\n",&b);
n=strlen(s);
for(i=0;i<n;i++)
{ 
    if(s[i]==a && s[i+1]==b)
    {
      j++;
    }
}
printf("%d",j);
}

Input:
malayalam
a
l

Output:
2 

Explanation:
The two occurrences where a is followed by l is as highlighted below.