#include<stdio.h>
#include <stdlib.h>

int main()
{
    int n,a[999],i=0,count=0,j;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    i=0;
    j=n-1;
    while(i<n/2 && j>=n/2)
    {
        printf("%d ",a[i]+a[j]);
        i++;
        j--;
    }
}