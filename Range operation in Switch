#include<stdio.h>
#include <stdlib.h>
int main()
{
    
    int n;
    scanf("%d",&n);
    switch(n)<%
    // <% is used instead of {
    case 1 ... 9:// ... -to specify range
    printf("Single digit");
    break;
    case 10 ... 99:
    printf("Two digits");
    break;
    %>

    
}

