#include<stdio.h>
#include <stdlib.h>

int main()
{
    
    unsigned long long int n;
    int x,count=0,temp,l,p=1;
    scanf("%llu %d",&n,&x);
    while(x--)
    {
        p*=10;
    }
    l=n%p;
    while(n>p-1)
    {
       
        n/=10;
    }
   
    printf(n==l ? "Yes" : "No");


}