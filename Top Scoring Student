#include<stdio.h>
#include <stdlib.h>

int main()
{int n,m1,m2,m3,i,t[50],l=0;
char s[100],top[100];
scanf("%d",&n);
for(i=0;i<n;i++)
{scanf("%[^:]:%d:%d:%d",s,&m1,&m2,&m3);
t[i]=m1+m2+m3;
if(t[i]>l)
{l=t[i];
 strcpy(top,s);
}
}
printf("%s",top);

}