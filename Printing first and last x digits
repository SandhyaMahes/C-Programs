#include<stdio.h>
#include <stdlib.h>

int main()
{
    int n,x,i,count,temp,l,f,p;
    scanf("%d",&n);
    scanf("%d",&x);
    temp=n;
    while(temp!=0)
    {
        temp=temp/10;
        count++;
    }
    p=pow(10,(count-x));
    printf("%d\n",n/p);
    p=pow(10,x);
    printf("%d",n%p);
}